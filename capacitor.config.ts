import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-tfjs-classifier',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
